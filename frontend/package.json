{
  "name": "powerpilot-panel",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "PowerPilot custom Home Assistant sidebar panel (Lit + esbuild).",
  "scripts": {
    "build": "node esbuild.mjs",
    "watch": "node esbuild.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "lit": "^3.2.1"
  }
}
